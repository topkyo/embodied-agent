import type { DeviceRegistry } from "@embodied-agent/core";

export function buildAquacultureSceneContext(
  _registry: DeviceRegistry,
  _deployment_id: string,
): { scene_context_sections: string[] } {
  return { scene_context_sections: [] };
}
