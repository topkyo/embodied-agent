# 本地运行态归档记录

归档时间：2026-06-14T01:29:02Z

基线提交：`dcc5260`

源路径：`apps/api/data`

目标路径：`.agentstack/archive/runtime-data/20260614T012902Z/apps-api-data/`

说明：本记录只记录本机运行态搬迁路径，不包含 settings、token、用户、微信同步文件或其他敏感内容。

恢复方式：

```bash
export AGENT_DATA_DIR=/path/to/explicit/data-dir
mkdir -p "$AGENT_DATA_DIR"
cp -a .agentstack/archive/runtime-data/20260614T012902Z/apps-api-data/. "$AGENT_DATA_DIR"/
```

不要恢复为 `apps/api/data` 默认态。需要旧数据时，必须显式设置 `AGENT_DATA_DIR`。
